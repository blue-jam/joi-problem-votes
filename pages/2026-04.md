---
layout: page
title: "2026年4月 投票結果"
---

日本情報オリンピックで印象に残っている問題の投票結果です。

{% assign results = site.data.votes["2026-04"] %}

## ランキング

| 順位 | 問題名 | 出典 | 票数 |
| :---: | :--- | :--- | :---: |
{% for problem in results %}| {{ problem.rank }} | {% if problem.source_url != "" %}[{{ problem.name | escape }}]({{ problem.source_url | escape }}){% else %}{{ problem.name | escape }}{% endif %} | {{ problem.source | escape }} | {{ problem.votes }} |
{% endfor %}

## コメント

{% for problem in results %}
{% if problem.comments.size > 0 %}
### {% if problem.source_url != "" %}[{{ problem.name | escape }}]({{ problem.source_url | escape }}){% else %}{{ problem.name | escape }}{% endif %} {% if problem.source != "" %}（{{ problem.source | escape }}）{% endif %}

{% for comment in problem.comments %}
- **{{ comment.nickname | escape }}**: {{ comment.reason | escape }}
{% endfor %}
{% endif %}
{% endfor %}
